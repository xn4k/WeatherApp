/**
 * Stable public facade for the forecast interpretation engine.
 *
 * UI components should import from this file. The implementation lives in
 * `lib/interpretation`, where every subject area is versioned and testable on
 * its own.
 */
export * from './interpretation'
